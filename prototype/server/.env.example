# Copy to .env and fill in. Never commit .env.
DATABASE_URL=postgres://dhansarthi:dhansarthi@localhost:5433/dhansarthi
OPENAI_API_KEY=
LLM_PROVIDER=openai
BANK_SOURCE=fixtures
PORT=3001

# Real-time avatar. Without these the stage falls back to its own renderer.
SIMLI_API_KEY=
SIMLI_FACE_ID=
