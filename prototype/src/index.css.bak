/* ============ Dhan Sarthi design tokens ============ */
:root {
  /* IDBI-adjacent brand: deep teal + amber accent */
  --brand-900: #073d33;
  --brand-700: #0b5c4d;
  --brand-600: #0e6e5c;
  --brand-500: #12836e;
  --brand-100: #d9efe9;
  --brand-50: #edf7f4;
  --accent-600: #d96c14;
  --accent-500: #f0821e;
  --accent-100: #fdeadb;

  /* ink + surfaces (warm paper, fixed — the future self writes on ivory, not white) */
  --surface-1: #f6f3ea;
  --surface-2: #ece8db;
  --card: #fffdf8;
  --ink-1: #1b1a13;
  --ink-2: #5b584c;
  --ink-3: #928d7d;
  --hairline: #e5e0d0;

  --font-display: 'Fraunces', Georgia, 'Times New Roman', serif;

  /* chart series (validated reference palette, fixed order) */
  --series-1: #2a78d6;
  --series-2: #1baf7a;
  --series-3: #eda100;
  --series-4: #008300;
  --series-5: #4a3aa7;

  /* status */
  --good: #0ca30c;
  --good-text: #006300;
  --warning: #fab219;
  --critical: #d03b3b;

  font-family: system-ui, -apple-system, "Segoe UI", sans-serif;
  -webkit-font-smoothing: antialiased;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  background:
    radial-gradient(900px 500px at 12% 108%, rgba(240, 130, 30, 0.14) 0%, transparent 60%),
    radial-gradient(1200px 800px at 70% -10%, #10352d 0%, #0a1f1a 55%, #071512 100%);
  color: var(--ink-1);
  min-height: 100vh;
}

/* ============ desktop stage around the phone ============ */
.stage {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 56px;
  padding: 32px 24px;
  flex-wrap: wrap;
}
.stage-copy { max-width: 380px; color: #e9f2ef; }
.stage-copy .kicker {
  display: inline-block; font-size: 12px; letter-spacing: 0.14em;
  text-transform: uppercase; color: #8fd0be; margin-bottom: 14px;
  border: 1px solid rgba(143, 208, 190, 0.35); padding: 5px 10px; border-radius: 999px;
}
.stage-copy h1 {
  font-family: var(--font-display); font-size: 38px; line-height: 1.12;
  margin-bottom: 16px; font-weight: 600; letter-spacing: -0.01em; text-wrap: balance;
}
.stage-copy h1 em { font-style: italic; font-weight: 560; color: #ffb26b; }
.stage-copy p { font-size: 15px; line-height: 1.55; color: #b9cfc8; margin-bottom: 10px; }
.stage-copy .pillars { margin-top: 18px; display: grid; gap: 10px; }
.stage-copy .pillar {
  display: flex; gap: 10px; align-items: flex-start; font-size: 13.5px;
  color: #d5e5df; line-height: 1.45;
}
.stage-copy .pillar b { color: #fff; }
.stage-copy .pillar .dot {
  flex: none; width: 8px; height: 8px; border-radius: 50%;
  background: var(--accent-500); margin-top: 6px;
}

/* ============ phone frame ============ */
.phone {
  width: 392px; height: 780px; flex: none;
  background: #0d0d0d; border-radius: 44px; padding: 10px;
  box-shadow: 0 30px 80px rgba(0,0,0,0.55), 0 0 0 1px rgba(255,255,255,0.08);
}
.screen {
  position: relative; width: 100%; height: 100%;
  background: var(--surface-1); border-radius: 34px; overflow: hidden;
  display: flex; flex-direction: column;
}
/* speaker notch */
.screen::before {
  content: ''; position: absolute; top: 9px; left: 50%; transform: translateX(-50%);
  width: 92px; height: 20px; border-radius: 999px; background: #0d0d0d; z-index: 60;
}
@media (max-width: 520px) {
  .stage { padding: 0; gap: 0; }
  .stage-copy { display: none; }
  .phone { width: 100vw; height: 100dvh; border-radius: 0; padding: 0; box-shadow: none; }
  .screen { border-radius: 0; }
}

/* ============ app chrome ============ */
.appbar {
  flex: none; display: flex; align-items: center; gap: 10px;
  padding: 38px 18px 12px;
}
.appbar .bank-mark {
  width: 34px; height: 34px; border-radius: 10px;
  background: linear-gradient(135deg, var(--brand-600), var(--brand-900));
  color: #fff; display: grid; place-items: center; font-weight: 800; font-size: 13px;
}
.appbar .titles { line-height: 1.15; }
.appbar .titles .t1 { font-size: 15px; font-weight: 700; }
.appbar .titles .t2 { font-size: 11px; color: var(--ink-3); }
.appbar .spacer { flex: 1; }
.voice-toggle {
  border: 1px solid var(--hairline); background: var(--card); border-radius: 999px;
  font-size: 11px; padding: 6px 10px; color: var(--ink-2); cursor: pointer;
  display: flex; align-items: center; gap: 5px; font-family: inherit;
}
.voice-toggle.on { border-color: var(--brand-600); color: var(--brand-700); background: var(--brand-50); }

.body { flex: 1; overflow-y: auto; padding: 4px 18px 18px; scrollbar-width: none; }
.body::-webkit-scrollbar { display: none; }

.tabbar {
  flex: none; display: flex; gap: 4px; border-top: 1px solid var(--hairline);
  background: var(--card); padding: 8px 8px calc(10px + env(safe-area-inset-bottom));
}
.tabbar button {
  flex: 1; background: none; border: 0; cursor: pointer; border-radius: 12px;
  display: flex; flex-direction: column; align-items: center; gap: 3px;
  font-size: 10.5px; color: var(--ink-3); padding: 6px 0 5px; font-family: inherit;
  transition: background 0.2s ease;
}
.tabbar button.active { color: var(--brand-700); font-weight: 700; background: var(--brand-100); }
.tabbar .ticon { font-size: 19px; line-height: 1; }

/* ============ shared bits ============ */
.h-section {
  font-size: 10.5px; font-weight: 800; color: var(--ink-3); margin: 20px 2px 10px;
  letter-spacing: 0.14em; text-transform: uppercase;
}
.card {
  background: var(--card); border: 1px solid var(--hairline); border-radius: 18px;
  padding: 14px 16px; margin-bottom: 10px;
  box-shadow: 0 1px 2px rgba(27, 26, 19, 0.04), 0 10px 26px -18px rgba(27, 26, 19, 0.22);
}
.pill {
  display: inline-flex; align-items: center; gap: 5px;
  font-size: 11px; font-weight: 600; padding: 4px 9px; border-radius: 999px;
}
.pill.good { background: #e7f6e7; color: var(--good-text); }
.pill.brand { background: var(--brand-100); color: var(--brand-700); }
.pill.accent { background: var(--accent-100); color: var(--accent-600); }

.btn {
  border: 0; border-radius: 12px; font-family: inherit; cursor: pointer;
  font-size: 14px; font-weight: 700; padding: 13px 16px; width: 100%;
  transition: transform 0.06s ease;
}
.btn:active { transform: scale(0.98); }
.btn.primary {
  background: linear-gradient(160deg, var(--brand-500), var(--brand-700));
  color: #fff; box-shadow: 0 10px 22px -10px rgba(11, 92, 77, 0.65);
}
.btn.ghost { background: var(--brand-100); color: var(--brand-700); }

/* ============ onboarding chat ============ */
.chat-scroll { display: flex; flex-direction: column; gap: 10px; padding-bottom: 8px; }
.msg { max-width: 84%; padding: 10px 13px; border-radius: 16px; font-size: 13.5px; line-height: 1.45; animation: rise 0.25s ease; }
.msg.bot { background: var(--card); border: 1px solid var(--hairline); border-bottom-left-radius: 5px; align-self: flex-start; }
.msg.user { background: var(--brand-600); color: #fff; border-bottom-right-radius: 5px; align-self: flex-end; }
.msg .fineprint { display: block; margin-top: 6px; font-size: 10.5px; color: var(--ink-3); }
@keyframes rise { from { opacity: 0; transform: translateY(6px); } to { opacity: 1; transform: none; } }

.chips { display: flex; flex-wrap: wrap; gap: 8px; padding: 10px 0 4px; animation: rise 0.25s ease; }
.chips button {
  border: 1.5px solid var(--brand-600); color: var(--brand-700); background: var(--card);
  border-radius: 999px; padding: 9px 14px; font-size: 13px; font-weight: 600;
  cursor: pointer; font-family: inherit;
}
.chips button:active { background: var(--brand-50); }

.typing { display: inline-flex; gap: 4px; padding: 12px 14px; }
.typing i { width: 6px; height: 6px; border-radius: 50%; background: var(--ink-3); animation: blink 1s infinite; }
.typing i:nth-child(2) { animation-delay: 0.15s; }
.typing i:nth-child(3) { animation-delay: 0.3s; }
@keyframes blink { 0%, 60%, 100% { opacity: 0.25; } 30% { opacity: 1; } }

.avatar-hello { display: flex; flex-direction: column; align-items: center; padding: 10px 0 6px; }
.avatar-hello .halo {
  width: 148px; height: 148px; border-radius: 50%; overflow: hidden;
  border: 3px solid var(--brand-100); box-shadow: 0 8px 24px rgba(14, 110, 92, 0.18);
  background: #dcefe9;
}

/* ============ dashboard ============ */
.hero {
  background:
    radial-gradient(320px 200px at 88% -20%, rgba(240, 130, 30, 0.5) 0%, transparent 65%),
    linear-gradient(140deg, var(--brand-700), var(--brand-900));
  color: #fff; border-radius: 20px; padding: 18px 16px; margin-bottom: 10px;
  box-shadow: 0 16px 34px -18px rgba(7, 61, 51, 0.55);
}
.hero .label { font-size: 10px; color: #9fd2c4; margin-bottom: 6px; letter-spacing: 0.12em; text-transform: uppercase; font-weight: 700; }
.hero .big { font-family: var(--font-display); font-size: 34px; font-weight: 600; letter-spacing: -0.01em; }
.hero .sub { font-size: 12px; color: #cfe7df; margin-top: 6px; line-height: 1.45; }
.hero .sub b { color: #ffc188; }

.barlist .row { display: grid; grid-template-columns: 88px 1fr 62px; gap: 10px; align-items: center; margin-bottom: 9px; }
.barlist .name { font-size: 12px; color: var(--ink-2); }
.barlist .track { height: 14px; border-radius: 4px; background: var(--surface-2); overflow: hidden; }
.barlist .fill { height: 100%; border-radius: 0 4px 4px 0; transition: width 0.5s ease; }
.barlist .val { font-size: 12px; text-align: right; font-variant-numeric: tabular-nums; color: var(--ink-1); }

.nudge { display: flex; gap: 11px; align-items: flex-start; }
.nudge .ic {
  flex: none; width: 36px; height: 36px; border-radius: 11px; display: grid; place-items: center;
  font-size: 17px; background: var(--brand-50);
}
.nudge .tx { font-size: 12.5px; line-height: 1.45; color: var(--ink-2); }
.nudge .tx b { color: var(--ink-1); }
.nudge .act { margin-top: 7px; }
.nudge .act button {
  border: 0; background: none; color: var(--brand-700); font-weight: 700; font-size: 12px;
  cursor: pointer; padding: 0; font-family: inherit;
}

/* ============ future self planner ============ */
.fs-stagebox {
  position: relative; border-radius: 20px; overflow: hidden;
  border: 1px solid var(--hairline); margin-bottom: 12px; background: var(--card);
  box-shadow: 0 16px 34px -22px rgba(27, 26, 19, 0.4);
}
.fs-caption {
  position: absolute; left: 12px; right: 12px; bottom: 12px;
  background: rgba(12, 24, 20, 0.8); backdrop-filter: blur(6px);
  color: #f4efe2; border-radius: 13px; padding: 11px 13px;
  font-family: var(--font-display); font-style: italic; font-size: 13.5px; line-height: 1.5;
}
.fs-caption b { color: #ffc188; font-style: normal; }
.fs-age {
  position: absolute; top: 12px; left: 12px;
  background: rgba(12, 24, 20, 0.72); color: #fff; border-radius: 999px;
  font-size: 11px; font-weight: 700; padding: 5px 11px;
}

.slider-card {
  background: var(--card); border: 1px solid var(--hairline); border-radius: 18px;
  padding: 14px 16px; margin-bottom: 10px;
  box-shadow: 0 1px 2px rgba(27, 26, 19, 0.04), 0 10px 26px -18px rgba(27, 26, 19, 0.22);
}
.slider-head { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 4px; }
.slider-head .lab { font-size: 12px; color: var(--ink-2); }
.slider-head .amt { font-family: var(--font-display); font-size: 22px; font-weight: 600; color: var(--brand-700); }
input[type="range"] {
  width: 100%; margin: 12px 0 4px; appearance: none; height: 6px; border-radius: 3px;
  background: linear-gradient(to right, var(--brand-600) var(--pct, 30%), var(--surface-2) var(--pct, 30%));
  outline: none;
}
input[type="range"]::-webkit-slider-thumb {
  appearance: none; width: 26px; height: 26px; border-radius: 50%;
  background: #fff; border: 3px solid var(--brand-600);
  box-shadow: 0 0 0 5px rgba(240, 130, 30, 0.22), 0 2px 8px rgba(0,0,0,0.22); cursor: pointer;
}
input[type="range"]::-moz-range-thumb {
  width: 22px; height: 22px; border-radius: 50%;
  background: #fff; border: 3px solid var(--brand-600);
  box-shadow: 0 2px 8px rgba(0,0,0,0.2); cursor: pointer;
}
.slider-scale { display: flex; justify-content: space-between; font-size: 10.5px; color: var(--ink-3); }

.proj { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 10px; }
.stat {
  background: var(--card); border: 1px solid var(--hairline); border-radius: 15px; padding: 12px 14px;
  box-shadow: 0 1px 2px rgba(27, 26, 19, 0.04);
}
.stat .k { font-size: 10.5px; color: var(--ink-3); margin-bottom: 4px; }
.stat .v { font-family: var(--font-display); font-size: 20px; font-weight: 600; letter-spacing: -0.01em; }
.stat .v.good { color: var(--good-text); }

.why { border-left: 3px solid var(--accent-500); }
.why summary { cursor: pointer; font-size: 12.5px; font-weight: 700; color: var(--ink-1); list-style: none; }
.why summary::before { content: "💡 "; }
.why p { font-size: 12px; color: var(--ink-2); line-height: 1.5; margin-top: 8px; }
.why .basis { margin-top: 8px; display: flex; flex-wrap: wrap; gap: 6px; }

/* ============ advisor tab ============ */
.rm-card { display: flex; gap: 12px; align-items: center; }
.rm-card .face {
  width: 42px; height: 42px; border-radius: 50%; background: var(--accent-100);
  display: grid; place-items: center; font-size: 20px;
}
.rm-card .tx { font-size: 12px; line-height: 1.4; color: var(--ink-2); flex: 1; }
.rm-card .tx b { color: var(--ink-1); font-size: 13px; }

.disclaimer {
  font-size: 10px; color: var(--ink-3); line-height: 1.5; text-align: center;
  padding: 10px 6px 2px;
}

/* ============ avatar idle life ============ */
.fs-person { animation: fs-breathe 3.8s ease-in-out infinite; }
@keyframes fs-breathe {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(1.6px); }
}
.fs-eyes { transform-box: fill-box; transform-origin: center; animation: fs-blink 4.6s infinite; }
@keyframes fs-blink {
  0%, 93%, 100% { transform: scaleY(1); }
  95%, 97% { transform: scaleY(0.08); }
}
.fs-gaze { animation: fs-look 9s ease-in-out infinite; }
@keyframes fs-look {
  0%, 52%, 100% { transform: translateX(0); }
  58%, 68% { transform: translateX(-2px); }
  74%, 84% { transform: translateX(1.6px); }
}

/* ============ life event chip ============ */
.life-event {
  display: flex; align-items: center; gap: 8px; flex-wrap: wrap;
  background: linear-gradient(120deg, var(--accent-100), var(--card));
  border: 1px solid #eecda0; border-radius: 14px;
  padding: 9px 12px; margin-bottom: 10px;
  animation: le-pop 0.5s cubic-bezier(0.2, 1.4, 0.4, 1);
}
@keyframes le-pop {
  from { opacity: 0; transform: translateY(8px) scale(0.96); }
  to { opacity: 1; transform: none; }
}
.life-event .le-icon { font-size: 17px; }
.life-event .le-label {
  font-size: 9px; font-weight: 800; letter-spacing: 0.08em; text-transform: uppercase;
  color: var(--accent-600); background: #fff; border-radius: 999px; padding: 3px 7px;
}
.life-event .le-text {
  flex: 1 1 100%; font-family: var(--font-display); font-style: italic;
  font-size: 13px; color: var(--ink-1); line-height: 1.45;
}

/* stat units + pinch styling */
.stat .v .unit {
  font-family: system-ui, -apple-system, "Segoe UI", sans-serif; font-style: normal;
  font-size: 10.5px; font-weight: 600; color: var(--ink-3); margin-left: 4px; letter-spacing: 0;
}
.stat .v.pinch { color: var(--ink-2); }

/* ============ 2057 prices card ============ */
.prices .p-head { font-size: 12px; font-weight: 700; color: var(--ink-1); margin-bottom: 9px; }
.prices .p-row-wrap { display: grid; gap: 6px; }
.prices .p-row {
  display: grid; grid-template-columns: 1fr auto auto auto; gap: 8px; align-items: baseline;
  font-size: 12px;
}
.prices .p-item { color: var(--ink-2); }
.prices .p-then { color: var(--ink-3); text-decoration: line-through; font-variant-numeric: tabular-nums; }
.prices .p-arrow { color: var(--accent-600); }
.prices .p-now { font-weight: 800; color: var(--ink-1); font-variant-numeric: tabular-nums; }
.prices .p-foot { margin-top: 9px; font-size: 10.5px; color: var(--ink-3); font-style: italic; }

/* ============ live voice call ============ */
.call-fab {
  position: absolute; right: 14px; bottom: 86px; z-index: 25;
  border: 0; border-radius: 999px; cursor: pointer; font-family: inherit;
  background: linear-gradient(135deg, var(--accent-500), var(--accent-600));
  color: #fff; font-size: 13px; font-weight: 800; padding: 12px 16px;
  box-shadow: 0 10px 26px rgba(217, 108, 20, 0.45);
  animation: fab-pulse 2.6s ease infinite;
}
@keyframes fab-pulse {
  0%, 100% { box-shadow: 0 10px 26px rgba(217,108,20,0.45); }
  50% { box-shadow: 0 10px 26px rgba(217,108,20,0.45), 0 0 0 9px rgba(240,130,30,0.14); }
}

.voice-overlay {
  position: absolute; inset: 0; z-index: 40;
  background: linear-gradient(165deg, #0c2b24 0%, #071512 70%);
  display: flex; flex-direction: column; align-items: center;
  padding: 26px 22px calc(20px + env(safe-area-inset-bottom));
  color: #e9f2ef; animation: rise 0.3s ease;
}
/* breathing edge glow while the mic is live — teal when listening, amber when speaking */
.v-ring {
  position: absolute; inset: 0; border-radius: inherit; pointer-events: none;
  opacity: 0; transition: opacity 0.5s ease; z-index: 1;
}
.v-ring.listening { opacity: 1; box-shadow: inset 0 0 52px -14px rgba(18, 131, 110, 0.95); animation: ring-breathe 2.6s ease-in-out infinite; }
.v-ring.speaking { opacity: 1; box-shadow: inset 0 0 52px -12px rgba(240, 130, 30, 0.85); animation: ring-breathe 1.6s ease-in-out infinite; }
@keyframes ring-breathe {
  0%, 100% { filter: brightness(1); }
  50% { filter: brightness(1.55); }
}

.v-close {
  position: absolute; top: 16px; right: 16px; z-index: 2;
  width: 34px; height: 34px; border-radius: 50%; border: 1px solid rgba(255,255,255,0.25);
  background: rgba(255,255,255,0.08); color: #fff; font-size: 14px; cursor: pointer;
}
.v-stage { display: flex; flex-direction: column; align-items: center; margin-top: 34px; }
.v-halo {
  width: 190px; height: 190px; border-radius: 50%; overflow: hidden;
  border: 3px solid rgba(240, 130, 30, 0.55); background: #dcefe9;
  transition: box-shadow 0.12s linear;
}
.v-halo svg { width: 190px; height: 190px; object-fit: cover; transform: scale(1.55) translateY(14px); }
.v-name { margin-top: 14px; font-size: 12px; font-weight: 700; color: #ffc188; letter-spacing: 0.04em; }
.v-status { margin-top: 6px; font-size: 13px; color: #9fd2c4; min-height: 18px; }
.v-status.speaking { color: #ffc188; }

.v-captions { flex: 1; width: 100%; display: flex; flex-direction: column; justify-content: flex-end; gap: 8px; padding: 18px 4px; overflow: hidden; }
.v-user { font-size: 12px; color: #8ba69e; text-align: right; font-style: italic; }
.v-bot {
  font-family: var(--font-display); font-style: italic;
  font-size: 16.5px; line-height: 1.55; color: #f3f8f6;
  max-height: 128px; overflow: hidden; display: flex; align-items: flex-end;
}

.v-typebar { display: flex; gap: 8px; width: 100%; margin-bottom: 10px; }
.v-typebar input {
  flex: 1; border: 1px solid rgba(255,255,255,0.2); border-radius: 999px;
  background: rgba(255,255,255,0.07); color: #fff; font-family: inherit;
  font-size: 12.5px; padding: 10px 15px; outline: none;
}
.v-typebar input::placeholder { color: #6f8a82; }
.v-typebar input:focus { border-color: rgba(240,130,30,0.6); }
.v-typebar button {
  width: 38px; border-radius: 50%; border: 0; cursor: pointer;
  background: var(--accent-500); color: #fff; font-size: 16px; font-weight: 800;
}
.v-typebar button:disabled { opacity: 0.35; cursor: default; }

.v-controls { display: flex; gap: 10px; width: 100%; }
.v-btn {
  flex: 1; border: 1px solid rgba(255,255,255,0.22); border-radius: 13px;
  background: rgba(255,255,255,0.08); color: #fff; font-family: inherit;
  font-size: 13.5px; font-weight: 700; padding: 13px 0; cursor: pointer;
}
.v-btn.warn { background: rgba(250, 178, 25, 0.2); border-color: rgba(250, 178, 25, 0.5); }
.v-btn.end { background: rgba(208, 59, 59, 0.85); border-color: transparent; }
.v-finenote { margin-top: 10px; font-size: 9.5px; color: #6f8a82; text-align: center; line-height: 1.5; }

.v-keysheet { margin: auto 0; width: 100%; background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.14); border-radius: 18px; padding: 22px 18px; }
.v-keyhead { font-family: var(--font-display); font-size: 19px; font-weight: 600; margin-bottom: 10px; }
.v-keysheet p { font-size: 12.5px; line-height: 1.55; color: #b9cfc8; margin-bottom: 14px; }
.v-keysheet p b { color: #fff; }
.v-keysheet input {
  width: 100%; border: 1px solid rgba(255,255,255,0.25); border-radius: 11px;
  background: rgba(0,0,0,0.3); color: #fff; font-family: inherit; font-size: 13px;
  padding: 12px 13px; margin-bottom: 12px; outline: none;
}
.v-keysheet input:focus { border-color: var(--accent-500); }
.v-keysheet .btn:disabled { opacity: 0.45; cursor: not-allowed; }

/* toast */
.toast {
  position: absolute; left: 16px; right: 16px; bottom: 84px; z-index: 30;
  background: var(--ink-1); color: #fff; border-radius: 13px; padding: 13px 15px;
  font-size: 12.5px; line-height: 1.45; animation: rise 0.3s ease;
  box-shadow: 0 12px 30px rgba(0,0,0,0.3);
}
.toast b { color: #8fd0be; }
