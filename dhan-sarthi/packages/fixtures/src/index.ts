/**
 * Synthetic customers and the product shelf.
 *
 * Nothing real ever lives here. The declaration signed with IDBI forbids production or customer
 * data in the sandbox, so this package is the only source of account data until BANK_SOURCE=idbi
 * points at their APIs.
 *
 * Ports from archive/prototype/server/src/providers/bank.js (the fixtures branch).
 */
export {}
